<h1>Book <%= @book.title %></h1>
<p>Amount: <%= humanized_money_with_symbol(@book.price) %></p>

<%= form_tag orders_path do %>
  <%= hidden_field_tag 'book_id', @book.id %>
  <%= submit_tag 'Buy' %>
<% end %>

<form action='' method=''>
</form>
